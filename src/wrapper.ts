let GunW = class {
  /*
    Gun wrapper, simplifies creating, reading, writing,
    and updating data. Because every operation involves chaining .get()
    and .put()

    gun.get('something').get('not').get('exist').get('yet').put("Hello World!");
     `.put` will if needed, backwards create a document
     so "Hello World!" has a place to be saved.
  */

  constructor(peers: Array<string>) {
    /*
    It 

    */

  }

}
